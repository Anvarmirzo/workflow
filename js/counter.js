/* COUNTER */
window.addEventListener('scroll', function () {
    let w_top = window.pageYOffset;
    let e_top = document.getElementById('counter').offsetTop;
    console.log(`${e_top} ${w_top}`);
    // console.log(window);
});
/* COUNTER END */